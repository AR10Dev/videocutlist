# Stage 0 — Discovery and Coupling Inventory

**State:** PASS

## Failed gate 1

`STAGE FAIL`

- Frozen tracked inventory is 132 matching lines in 24 files, not 128/22;
  `.tasks/integration-risks.md` and `.tasks/stages/0.md` were omitted.
- `git diff --check` found trailing whitespace in the baseline report.
- The verifier passed Playwright 2/2 twice, so the earlier 1/2 result must be
  recorded as a non-reproduced observation rather than the current baseline.

Remediation commit: `4310a0a`.

## Failed gate 2

`STAGE FAIL`

- Controller `make check`, `make test`, and `make smoke` passed.
- Controller Playwright failed the primary save-flow assertion 1/2 after the
  re-verifier passed 2/2.
- Root cause is a test race: a pending preview status can replace the
  short-lived project-save status before the assertion observes it.
- Controller exception: a new remediation agent may stabilize only
  `web/playwright/segment-selection.spec.ts` and update the baseline evidence;
  application behavior remains prohibited.

Test-race remediation commit: `ecc02d3`.

## Final verdict

`STAGE PASS`

## Frozen scope

Discovery and documentation only. Do not change application, deployment, test,
or contract behavior.

## Required outputs

- `docs/refactor/tailscale-coupling-inventory.md`
- `docs/refactor/current-network-assumptions.md`
- `docs/refactor/baseline-test-results.md`
- `docs/refactor/refactor-plan.md`
- `docs/adr/ADR-connectivity-provider-independence.md`
- `docs/verification/stage-0-report.md`

## Acceptance criteria

1. Every Tailscale/tailnet import, header, configuration value, command,
   environment variable, deployment file, health check, test, and
   documentation reference is inventoried and classified.
2. Same-origin, localhost-only, listener, proxy-trust, authentication, and
   frontend API URL assumptions are documented.
3. The current build, test, smoke, and Playwright baselines include exact
   commands, versions, results, and limitations.
4. The refactor plan maps the minimal Stage 1–7 sequence and identifies frozen
   contracts that later stages must replace.
5. The ADR makes connectivity providers optional and records that Stage 0 does
   not change runtime behavior.
6. A separate Terra verifier confirms completeness and the controller
   independently inspects the diff, searches the repository, and reruns the
   required commands.
7. No unrelated or behavior-changing file is modified.

## Required controller commands

```text
make check
make test
make smoke
npm --prefix web run test:e2e
```
